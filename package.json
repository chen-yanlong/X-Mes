{
  "name": "x-mes",
  "private": true,
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "install": "lerna bootstrap",
    "build": "lerna run build",
    "frontend": "yarn workspace frontend dev"
  },
  "dependencies": {},
  "devDependencies": {
    "lerna": "^8.1.8"
  }
}
